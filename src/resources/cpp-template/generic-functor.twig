{%- import 'M.twig' as M -%}

template <{{ M.tHeader([0..functor.getSize()-1], 'class T%d', 'int _i') }}>
struct {{ M.subFunctorId(functor) }} {
    template <int which>
    {% ol %}
    using v = typename at<which
        {% for i in [0..functor.getSize()-1] -%}
            , pack<>
        {%- endfor -%}
    >::t;
    {% endol %}
};
