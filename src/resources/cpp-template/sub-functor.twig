template<>
struct _{{functor.getId()}}_{{functor.getSize()}} <
    {%- for number in fact.getArguments() -%}
    Atom<{{ context.getConstantIndex(number.getInt()) }}>,
    {%- endfor -%}
    {{- functor.getItemCount() -}}
> {
    {% for number in fact.getArguments() -%}
    typedef pack<{{ context.getConstantIndex(number.getInt()) }}> v{{ loop.index0 }};
{% endfor -%}
};

